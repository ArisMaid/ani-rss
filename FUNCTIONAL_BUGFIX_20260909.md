# 评分排序与首页订阅显示：修复方案及实施记录

> 日期：2026-09-09。基准：本地 `066fccc8`，业务代码来自 v3.2.28.66。
> 用户提供的两张截图为问题证据。本轮已修改本地代码，未提交、推送、打标签或部署。

## 1. 处理结论

两项用户报告的问题已定位并修复，同时处理了相邻的首页跨天日期不更新问题。没有修改后端、下载策略、缓存持久化、用户订阅数据或评分接口。

| 问题 | 原因 | 已实施修复 |
| --- | --- | --- |
| Mikan 评分乱序 | 服务端仅按列表请求时已有缓存评分排序，后续评分批次只更新数值；模板继续沿用初始顺序 | computed 展示副本按最新评分降序，条目用 URL 作为 key |
| 首页有数量但无卡片 | 模板用了 AniCoverView，但 script 没导入；本项目自动组件插件并不扫描 src/view 下的该组件 | 用已有 defineAsyncComponent 方式显式注册，只有非空当天列表才触发卡片加载 |
| 首页跨天仍显示昨天 | todayLabel 的 computed 仅调用 new Date，没有响应式依赖；疑似停更天数也依赖不可追踪的 Date.now | 引入一个响应式 currentTime，在已有请求/刷新/可见恢复周期更新，供星期与停更天数计算使用 |
| “排序”设置范围不清楚 | 设置项名过于泛化，易被理解为所有外部来源列表都受控 | 改名“订阅排序”，不增加第二套排序配置 |

## 2. Mikan 排序的具体方案

### 问题链路

`MikanService` 在列表响应前合并当时已有的缓存评分并排序。冷缓存、部分评分已缓存、不同批次返回速度不同时，初始顺序都可能与最终评分顺序不同。

前端 `MikanView.startScores` 的 onUpdate 使用 mergeScores 更新原条目；原模板直接遍历 data.weeks/ week.items，因此只更新显示的数值，不移动条目。用户截图中的 7.0 在 7.2 前、7.8 在 5.5 后，符合这条链路。

### 已实施方式

修改 `ani-rss-ui/src/view/home/MikanView.vue`：

1. 增加 computed `displayWeeks`，各星期保留原元数据，只复制 items 数组再排序。
2. 有限非负数值按分数降序；null、缺失和非法数值归为未知，排在有效评分之后。同分保持源数组顺序。
3. computed 直接读取响应式 item.score，任何部分批次更新都会重新计算可见顺序，不依赖“所有请求都成功完成”。
4. 模板改用 displayWeeks，保留 data.weeks 作为数据和补载状态的来源；不重建 API 对象或重启请求。
5. collapse 条目增加 `:key="it.url"`，使重排跟随稳定条目身份，减少展开内容错位风险。

产品约定：Mikan 沿用原服务端的星期内评分降序语义；评分陆续返回时位置可以变化，未知分数置后。页面设置的拼音/更新时间排序用于订阅列表，不扩展成外部源的通用排序服务。

不新增排序缓存、排序 worker、服务端等待全部评分、全量列表重拉或全局设置同步机制。一次轻量 computed 足以处理这个局部问题。

## 3. 首页订阅卡片的具体方案

### 问题链路

`todayAnis` 同时驱动标题数量和卡片遍历。截图标题与分区数量均为 2，说明数据已经进入该计算结果，问题在渲染之后。

DashboardView 模板里的 AniCoverView 未注册。构建可以成功，但这个标签不能实例化真实 Vue 卡片，最终只有外层空节点；因此增加容器高度或改变星期过滤不能解决问题。

### 已实施方式

在 `ani-rss-ui/src/view/home/DashboardView.vue` 使用：

```js
const AniCoverView = defineAsyncComponent(() => import('@/view/home/AniCoverView.vue'))
```

继续使用已有组件的评分、封面、编辑、播放和删除事件，不复制一套首页卡片。非空 todayAnis 才挂载卡片，空列表保留原空态；不引入轮播库、额外数据接口或布局补丁。

异步组件冷加载需要一次现有 Vite chunk 请求，这是实际卡片渲染所需成本。不能以让首页继续空白的方式维持原体积数字。

## 4. 相邻功能审查：跨天日期更新

### 原因与修复

没有响应式依赖的 `computed(() => new Date().getDay())` 会缓存结果。打开首页跨过午夜后，即使下载器列表轮询，星期和 todayAnis 也可能继续停留在昨天。停更天数也存在类似“仅真实时间流逝不能触发重算”的问题。

新增 currentTime ref，`loadTorrents` 入口更新 Date.now；todayLabel 和 procrastinatingDays 读取它。现有 5 秒轮询、手动刷新、激活与可见恢复路径均可带动时钟更新。只需更新时间，不等待网络请求成功。

采用浏览器本地日期，保持现有产品规则；不增加时区配置、午夜定时器或后端日历服务。页面隐藏期间允许暂停显示更新，返回后按既有周期刷新。

这项修复不改变后台对订阅星期的分类，不等于自动重新取得所有订阅数据。实际新增/修改订阅后的列表更新仍沿用现有流程；用户可使用首页刷新。

## 5. 为什么以往的绿色检查没有发现

- 原首页轮询用例主要验证请求调度，fixture 是空 weekList，并将 AniCoverView stub 成占位组件，不能证明真实组件导入正确。
- 原补载检查主要验证数值合并与请求生命周期，没有直接验证最终可见条目顺序。
- 生产构建成功只说明模块可以构建，不能替代非空数据页面检查。

本轮按用户要求不新增永久单元/回归测试。补充一次真实生产构建页面检查，使用隔离 HTTP fixture，验证“有实际卡片”和“延迟评分后的最终顺序”。这不是新增长期浏览器矩阵。

## 6. 已执行验证

1. 现有前端测试：5 个文件，35/35 通过；修改为按需卡片加载后再次通过。
2. 生产构建：`pnpm exec vite build --outDir target/bugfix-20260909-dist --emptyOutDir false`，通过；未清理既有输出目录。
3. 浏览器：现有 W7 HTTP fixture 配合本次临时请求拦截，首页提供两个当天启用订阅，真实页面显示两张卡片，标题数量也是 2。没有真实封面时组件显示原有占位图，仍正常展示名称、评分和进度。
4. Mikan：初始数据 A=8.5、B=未知、C=7.2；延迟 1.5 秒返回 B=9.1，页面显示顺序变为 B 9.1、A 8.5、C 7.2。
5. 跨天：使用浏览器时钟模拟下一日并点击刷新，页面从“星期三 · 2 个订阅”变为“星期四 · 没有订阅”，符合 fixture 仅星期三有数据的事实。
6. 最终 bundle 检查通过，三个场景 forbiddenModules 均为空；播放器、Markdown、备份等禁止模块仍未进入被检查的静态加载集合。

临时浏览器操作脚本放在已忽略的 target 中，没有新增正式测试文件。验证截图位于仓库 `output/playwright/home-fixed.png` 和 `output/playwright/mikan-fixed.png`。本轮只验证本地生产 UI 与合成 API 数据，未连接真实下载器、账号或外部番剧源。

## 7. 构建预算的必要调整

恢复真实组件会改变共享模块拆分；首页不能继续以漏掉卡片的构建作为完全不变的预算。先尝试静态导入，发现会将卡片依赖提前带进登录/首页，因此最终改为按需加载。

最终实测与明确上限如下（gzip 字节）：

| 场景 | JS 实测 | JS 上限 | CSS 实测 | CSS 上限 |
| --- | ---: | ---: | ---: | ---: |
| 静态入口 | 105227 | 105500 | 48953 | 48953 |
| 登录 | 202044 | 202500 | 63229 | 原规则未设独立上限 |
| 首页 | 192264 | 192500 | 61822 | 61822 |
| 订阅 | 186271 | 186500 | 63912 | 64000 |

相较旧上限，JS 调整分别为 299、544、326、1245 字节；订阅 CSS 调整 355 字节。采用小幅取整余量，避免正常模块 hash 和压缩变化导致几字节误报；没有删除检查或自动随构建放宽上限。

记录位置为 `ani-rss-ui/scripts/bundle-budget.json` 的 functionalRepair；原历史基线保留。注意首页静态加载集合不含随后按需请求的卡片，不能把这一数字宣传为完整非空首页网络开销或性能提升。

## 8. 发布前要求与停止条件

- 本轮修改保持局部，不需要 Java 改动、数据库迁移或缓存重建。
- 发布者正常构建新前端并打包进 JAR/镜像；现有线上 v3.2.28.66 不会因为本地代码修改自动更新。
- 更新后快速查看非空“今天的订阅”和冷评分补齐后的顺序即可；沿用现有发布门禁，不新增覆盖率任务。
- 若旧页面长期打开，更新后重新加载页面取得新构建；无需清理订阅、配置或图片目录。
- 不自动提交、推送、修改版本号或发布；本轮交付为可审阅的本地代码、说明和验证结果。
- 后续若出现其他功能问题，按具体操作路径处理。不能把本轮相邻审查理解为全系统所有业务已经全面验证。
