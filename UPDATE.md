- feat: 同步上游 v3.2.19 至 v3.2.20 的路由化 WebUI、首页仪表盘、独立订阅/下载/日志/设置页面、封面与卡片布局、移动端导航、启动页设置和日志刷新修复；适配当前 fork 的 Cookie + CSRF 会话认证与安全图片缓存，版本升级为 v3.2.20.56
- feat/fix: 同步上游 v3.2.7 至 v3.2.18 的 qBittorrent 内容布局、WebUI 上传/更新、TMDB 年份路径变量、播放器与日期兼容修复；保留 fork 本地安全与下载可靠性改动，版本升级为 v3.2.18.55
- fix: 重构遗漏集补全策略，按集数、字幕组、分辨率与有效版本命名特征识别同版本资源，避免新 hash 反复下载；新增全局开关，并协调备用 RSS、共存、过滤、重命名、延迟下载与下载数量限制，版本升级为 v3.2.6.54
- perf: 清理 UI 嵌入资源复制目标，避免历史构建产物进入 JAR；Mikan 缓存预热延迟到“添加订阅”对话框打开后，版本升级为 v3.2.6.53
- feat/fix: 同步上游 v3.2.2 至 v3.2.6 的 Transmission 暂停与重命名修复、Telegram 聊天选择、Emby 字段兼容、自定义 CSS/JS 输出、前端依赖与构建优化；保留 fork 本地安全与性能改动，版本升级为 v3.2.6.52
- perf: 精简认证、任务归属、缺集恢复、媒体句柄、安全图片与构建热路径；首次部署账号密码统一脱敏为 admin/admin，移除内置 TMDB key，并缩小 Docker 构建上下文，版本升级为 v3.2.2.51
- fix: 修复 qBittorrent 完成重命名并自动删任务后，已跟踪资源可能绕过补全状态机而重复下载；删除前持久校验最终文件清单，并兼容旧备份的种子缓存，版本升级为 v3.2.2.50
- fix: 整合上游 v3.2.2，自定义下载路径统一使用 customDownloadPathTemplate，并更新 ical4j 与 swagger-core-jakarta，版本升级为 v3.2.2.49
- feat: 整合上游 v3.2.1，多背景图刮削兼容本地原子写入，Mikan 订阅搜索保留安全与性能优化，版本升级为 v3.2.1.48
- fix: 完成上游 v3.2.0 与本地版备份双向兼容，补齐逐文件无损校验、旧数据库迁移和缺失侧车重置，版本升级为 v3.2.0.47
- perf: Mikan 冷评分缩短中段轮询空档，后台结果可更快显示，版本升级为 v3.2.0.46
- perf: Mikan 季表加载后仅后台预取一个相邻季度，单线程、单飞且不递归，降低常见季度切换的上游等待，版本升级为 v3.2.0.44
- perf: Mikan 季表缓存到期后先返回最近快照并后台单飞刷新，避免常用季度阻塞等待上游，版本升级为 v3.2.0.43
- perf: Mikan 评分轮询对齐服务端 48 项批量上限，冷缓存会在有限窗口内持续更新，关闭选择器会立即取消未完成轮询，版本升级为 v3.2.0.42
- perf: 登录后的主界面提前预取 Mikan 当前季表并启动全季评分预热；评分映射与 SQLite 持久缓存写入解耦，避免缓存落盘阻塞可见评分，版本升级为 v3.2.0.41
- perf: 打开“添加订阅”窗口即预取当前 Mikan 季表；随后进入 Mikan 浏览器复用同一响应，首屏无需再等待第二次列表请求，版本升级为 v3.2.0.40
- perf: Mikan 封面安全抓取按代理配置复用有界 HTTP 连接池，同一 CDN 的缩略图可复用 TLS/TCP 连接并支持最多 16 个并发请求；DNS、重定向与私网校验保持逐请求执行，版本升级为 v3.2.0.39
- perf: Mikan 当前季表快照安全持久化 10 分钟，服务重启和重复打开不再重新等待上游季表；评分映射前导扫描改为轻量链接匹配，并将受控预热并发提升为 12 个映射与 4 个评分槽位，版本升级为 v3.2.0.38
- perf: Mikan 评分映射会流式扫描详情页前导内容，发现公开 Bangumi 链接后立即结束读取；巨大剧集表不再阻塞评分预热，仍保留完整页面回退兼容，版本升级为 v3.2.0.37
- fix: 删除订阅时，即使任务归属记录仍指向旧保存路径，也会清理当前下载模板可精确推导且确实为空的订阅目录；共享、非空、符号链接和下载根目录保持不动，版本升级为 v3.2.0.36
- fix: 删除 qBittorrent 订阅时，归属清单会覆盖所有已选种子文件（含封面、说明和字体），其后仅在目录确实为空时清理订阅目录；版本升级为 v3.2.0.35
- perf: Mikan 季表评分改为批量读取本地映射与评分缓存，冷项会先并发入队、后统一轮询；后续批次不再被前两批重试阻塞，版本升级为 v3.2.0.34
- fix: 删除没有下载归属记录的旧订阅时，也会清理其模板可精确推导且已为空的目录；共享、静态、非空、符号链接和不确定目录保持不动，版本升级为 v3.2.0.33
- fix: 删除订阅时，即使选择保留本地文件，也会清理已确认属于该订阅且已为空的目录；不会删除下载根目录、非空目录或完结迁移目录，版本升级为 v3.2.0.32
- perf: Mikan 与 Bangumi 公开评分缓存持久化到本地 SQLite，服务重启后已解析的作品不再重新抓取 Mikan 详情页；维护模式下自动暂停预热，版本升级为 v3.2.0.31
- perf: Mikan 列表封面改为进入可视区域后才请求本地安全图片缓存，避免整季封面请求抢占作品列表与评分加载；版本升级为 v3.2.0.30
- perf: Mikan 作品列表返回后以后台流水线解析公开评分，详情映射完成即进入 Bangumi 评分；前端按可见顺序小批量轮询，首屏不再等待整季评分完成，版本升级为 v3.2.0.29
- fix: 删除订阅且选择删除本地文件时，空的未完成任务目录也会按下载模板的静态根目录安全收口；不会删除下载根、共享目录、非空目录或归属不明的自定义静态路径，版本升级为 v3.2.0.28
- perf: Mikan 列表增加短时单飞缓存，评分批次改为受控并行；跨批次复用 Mikan/Bangumi 上游请求与缓存，版本升级为 v3.2.0.27
- fix: 删除订阅并选择删除本地文件后，会逐级清理仅属于该订阅且确认为空的目录；共享、含有其他内容或符号链接的目录保持不动，版本升级为 v3.2.0.26
- fix: 删除后重新添加相同订阅时，可安全重新登记已删除的下载归属并进入下载流程；旧归属和文件清单保留本地审计快照，版本升级为 v3.2.0.25
- fix: 删除订阅时支持选择是否删除本地文件；无法安全验证的历史归属文件会保留，不再阻断订阅删除，版本升级为 v3.2.0.24
- fix: 修复新增订阅首次提交因运行时对象失效而延迟，下载刷新改为按订阅 ID 合并单飞，版本升级为 v3.2.0.23
- fix: Mikan 评分接口标记并重试短暂失败的条目，版本升级为 v3.2.0.22
- fix: Mikan 季表切换隔离旧请求与折叠状态；公开评分分批加载，临时失败不再缓存为零分，版本升级为 v3.2.0.21
- fix: 订阅删除改为立即删除已确认归属的下载任务和本地文件，不再生成删除计划或隔离副本，版本升级为 v3.2.0.20
- feat: 同步上游 v3.1.75 至 v3.2.0 的功能行为，版本升级为 v3.2.0.19
- feat: qBittorrent 5.2+ 支持 ApiKey 授权，并兼容旧版用户名/密码
- feat: 支持使用 TMDB 原标题，原标题缺失时自动回退本地化标题
- refactor: 自定义下载路径使用 customDownloadPathTemplate，并兼容旧 downloadPath 配置
- fix: 修复任务列表不可修改、完成态误判及备用 RSS 多任务清理不完整问题
- fix: 下载器测试继续使用隔离配置；危险删除仍由本地归属与隔离机制接管
