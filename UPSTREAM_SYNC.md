# Fork 上游同步规则

本次 fork 修复以 `v3.2.28` 为上游基线，发布版本为 `3.2.28.60`。后续同步只审阅当前基线标签到目标标签之间的上游提交，不重复引入更早历史。

同步时必须保留以下本地合同：

1. 缺集恢复、SQLite 归属与备份迁移边界。
2. Cookie + CSRF 会话、401/403 语义、内网访问限制和 SSRF 防护。
3. 媒体短期句柄、外部播放器兼容和下载器失败不可伪装为空。
4. Mikan 列表先返回、按需/可取消评分、48 项批量上限和受控并发。
5. AnimeGarden subjects 先返回、当前星期按需补载、48 项数值 ID 限制。
6. 公共图片认证 GET、跨会话 SHA-256 缓存、ETag、TTL、失败冷却和 manifest 恢复。
7. 懒路由/懒弹窗、静态 hash 资源长缓存与 HTML/API 不缓存。
8. 验证和打包流程不得用 `clean` 删除既有输出；发布必须由 tag 工作流完成。
9. 版本号前三段跟随上游，第四段本地递增，并同步根 POM、模块 POM、前端默认版本与 `UPDATE.md`。

详细合同见 [`docs/fork-contracts.md`](docs/fork-contracts.md)，测量边界见 [`docs/performance-baseline.md`](docs/performance-baseline.md)。

Fork 版本号使用 `上游版本.本地修订号` 格式。例如 `3.2.1.48` 表示基于上游
`v3.2.1`，本地修订号为 `48`。

同步到新的上游版本时，必须完成以下审计：

1. 从当前 fork 版本号的前三段确定上游基线标签。
2. 只审阅该基线标签到目标标签之间的提交与差异，不重复同步更早的上游历史。
3. 比较正确上游标签范围，同时审查目标上游与 fork 的最终文件差异，不能仅看提交标题。
4. 按能力映射“上游旧入口 → 新入口 → fork API → 对应行为测试”；目录改名时仍迁移能力，不能只解决文本冲突。
5. 每项 fork 差异标记为“保留”“被上游等效替代”“明确淘汰”或“待确认”，并写出依据。不强制保留所有历史补丁，也不以冲突解决成功代替行为验证。
6. 同步 UI 时同时检查 `http.js` 使用点、v2/legacy 响应、媒体句柄、恢复状态与 package scripts；任何孤立 API 都要解释是否仍是兼容入口。同步下载/清理功能必须运行开发计划 T10.3 行为用例。
7. 同步构建时比较首屏模块图、包体预算和生产浏览器冒烟，保留白屏、`pageerror` 和 chunk 404 回归用例。
8. 版本沿用四段编号；当前优化不夹带 `v3.2.29`。后续同步自定义封面点击功能时，特别检查本地封面、远程封面、媒体句柄三个分支。
9. 每次同步提交附一页验收记录，至少包含功能、网络次数、首屏包、已验证项目和未验证项目；不能以“mvn 编译成功”代替页面正确。
10. 将版本号前三段更新为目标上游版本，并将第四段本地修订号递增一次；同步更新根 POM、模块父版本、前端默认版本、相关测试、`UPDATE.md` 与验收记录。

示例：从 `3.2.1.48` 同步上游 `v3.2.2` 时，只审阅
`v3.2.1..v3.2.2`，发布版本为 `3.2.2.49`。
